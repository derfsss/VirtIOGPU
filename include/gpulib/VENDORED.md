# Vendored gpu.library SDK headers

Copied from the P96_Replacement project (gpu.library SDK), API v0 FROZEN
2026-07-05 + append-only additions (GPU_FenceRetired, GPUTAG_AsyncFences).
Vendored per house pattern so this repo builds standalone. Re-sync from
P96_Replacement/sdk/include/ when the SDK gains appended slots/tags; the
frozen surface never changes shape.
